{
  "name": "github-actions-playground",
  "version": "2.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset:version": "changeset version && npm i",
    "changeset:add": "changeset add",
    "deploy": "changeset tag && ./scripts/tag.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@changesets/cli": "^2.29.4"
  }
}
